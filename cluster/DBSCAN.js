let noise = 0
let c = 0
let visited = new Set()
let clustered = new Set()
let clusters = {Noise: []}

function regionQuery(p){
    return []
}

function expandCluster(p, neighbours){
    if (!clusters)
}